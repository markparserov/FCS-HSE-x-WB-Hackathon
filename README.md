# FCS-HSE-x-WB-Hackathon

## Репликация wMAE = 0.370
Папка: `репликация метрики 0,370/` (подробности в APPROACH_SUMMARY.txt)

## Быстрый старт
```bash
cd "репликация метрики 0,370"
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
python replicate_370.py
